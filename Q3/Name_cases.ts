var person_name='jInNah';
// lower case
console.log(person_name.toLowerCase());
// upper case
console.log(person_name.toUpperCase());
//title case
var i=0;

var titleCase=person_name[i].toUpperCase();
var p=(person_name.toLowerCase());
console.log(titleCase);


for (i=1; i<person_name.length;i++)
{
        p=(person_name[i].toLowerCase());
        console.log(p);
}
